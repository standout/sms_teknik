require 'spec_helper'

describe SmsTeknik do
  it 'has a version number' do
    expect(SmsTeknik::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
